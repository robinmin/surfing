---
import { APP_BLOG } from 'astrowind:config';

import Grid from '~/components/blog/Grid.astro';
import Button from '~/components/ui/Button.astro';
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import type { Widget } from '~/types';
import { findLatestPosts } from '~/utils/blog';
import { getBlogPermalink } from '~/utils/permalinks';

export interface Props extends Widget {
    title?: string;
    linkText?: string;
    linkUrl?: string | URL;
    information?: string;
    count?: number;
}

const {
    title = await Astro.slots.render('title'),
    linkText = 'View all posts',
    linkUrl = getBlogPermalink(),
    information = await Astro.slots.render('information'),
    count = 4,

    id,
    isDark = false,
    classes = {},
    bg = await Astro.slots.render('bg'),
} = Astro.props;

// Get latest posts when blog is enabled (NO language filtering - client will filter)
const posts = APP_BLOG.isEnabled ? await findLatestPosts({ count: count * 3 }) : [];
const resolvedPosts = posts;
---

{
  APP_BLOG.isEnabled && resolvedPosts.length > 0 ? (
    <WidgetWrapper id={id} isDark={isDark} containerClass={classes?.container as string} bg={bg}>
      <div class="flex flex-col lg:justify-between lg:flex-row mb-8">
        {title && (
          <div class="md:max-w-sm">
            <h2
              class="text-3xl font-bold tracking-tight sm:text-4xl sm:leading-none group font-heading mb-2"
              set:html={title}
              data-i18n-key="pages.home.latestContent"
            />
            {APP_BLOG.list.isEnabled && linkText && linkUrl && (
              <Button variant="link" href={linkUrl} data-i18n-key="nav.browse">
                {' '}
                {linkText} »
              </Button>
            )}
          </div>
        )}

        {information && <p class="text-muted dark:text-slate-400 lg:text-sm lg:max-w-md" set:html={information} />}
      </div>

      <Grid posts={resolvedPosts} />
    </WidgetWrapper>
  ) : (
    <Fragment />
  )
}
