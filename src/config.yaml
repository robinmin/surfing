site:
  name: Surfing
  site: 'https://surfing.salty.vip'
  base: '/'
  trailingSlash: false

  googleSiteVerificationId: 7dkatua6POrB7ZU1eRzqCHJn_NFum6sU21AcXHRUoGQ

# Default SEO metadata
metadata:
  title:
    default: Surfing
    template: '%s — Surfing'
  description: '🏄‍♂️ AI-Powered Content Showcase Platform.'
  robots:
    index: true
    follow: true
  openGraph:
    site_name: Surfing
    images:
      - url: '@assets/images/surfing.png'
        width: 1200
        height: 630
        alt: 'Surfing - AI-Powered Content Showcase Platform'
    type: website
  twitter:
    handle: '@tangmian'
    site: '@tangmian'
    cardType: summary_large_image

i18n:
  language: en
  textDirection: ltr

apps:
  blog:
    isEnabled: true
    postsPerPage: 6

    post:
      isEnabled: true
      robots:
        index: true

    list:
      isEnabled: true
      pathname: 'articles' # Blog main path, you can change this to "articles" (/articles)
      robots:
        index: true

    category:
      isEnabled: true
      pathname: 'category' # Category main path /category/some-category, you can change this to "group" (/group/some-category)
      robots:
        index: true

    tag:
      isEnabled: true
      pathname: 'tag' # Tag main path /tag/some-tag, you can change this to "topics" (/topics/some-category)
      robots:
        index: false

    isRelatedPostsEnabled: true
    relatedPostsCount: 4

analytics:
  vendors:
    googleAnalytics:
      id: 'G-S3D3Q82DSX'
    microsoftClarity:
      id: 'topkp7sgz2'

cookieConsent:
  enabled: true

ui:
  theme: 'system' # Values: "system" | "light" | "dark" | "light:only" | "dark:only"

# Authentication configuration
auth:
  google_one_tap:
    enabled: true
  apple_sign_in:
    enabled: false
  # Token cache duration in seconds (default: 900 = 15 minutes)
  # Reduces unnecessary auth server checks by caching token validation
  token_cache_duration: 900

# Sentry error tracking configuration
sentry:
  enabled: true
  debug: false # Set to true to enable Sentry in development mode
  project: '4510129071783936' # Sentry project ID (extracted from DSN)
  org: '40fintech' # Sentry organization slug (extracted from auth token)
