# =============================================================================
# Zitadel OIDC Configuration (NEW - replaces Supabase/Google One Tap/Apple)
# =============================================================================
# Configure your Zitadel PKCE application in Zitadel Console
# Supports federated providers: Google, GitHub, Apple, Microsoft

# Your Zitadel instance URL (e.g., https://your-instance.zitadel.cloud)
PUBLIC_ZITADEL_AUTHORITY="https://auth.gobing.ai"

# PKCE Application Client ID (no secret needed for public clients)
PUBLIC_ZITADEL_CLIENT_ID="your-zitadel-pkce-client-id"

# Callback URL for OIDC redirects (must be registered in Zitadel Console)
PUBLIC_ZITADEL_REDIRECT_URI="https://surfing.salty.vip/auth/callback"

# Post-logout redirect URL
PUBLIC_ZITADEL_POST_LOGOUT_URI="https://surfing.salty.vip"

# IdP hint mappings (optional - provider IDs from Zitadel Console)
# These are the Zitadel IdP IDs for each federated provider
PUBLIC_ZITADEL_IDP_GOOGLE="google-idp-id"
PUBLIC_ZITADEL_IDP_GITHUB="github-idp-id"
PUBLIC_ZITADEL_IDP_APPLE="apple-idp-id"
PUBLIC_ZITADEL_IDP_MICROSOFT="microsoft-idp-id"

# Environment-Specific Notes:
# - LOCAL DEVELOPMENT: Store values in .env file (gitignored)
# - GITHUB ACTIONS: Add as Repository Secrets for build-time variables
# - CLOUDFLARE PAGES: Add as Environment Variables in dashboard for runtime variables

SENTRY_AUTH_TOKEN=""

# Sentry DSN (Optional - for custom error reporting)
# If not provided, will use hardcoded value from sentry.*.config.ts
PUBLIC_SENTRY_DSN=""
